# onigiri
X11 login screen manager in rust

build with nix
```bash
nix build
∙ ./result/bin/onigiri --config ./config.nix
```


build with cargo
```bash
  cargo run --features nix_config -- --config ./config.nix
```

use config.nix to configure now manager works.
